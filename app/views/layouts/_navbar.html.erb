<nav role="navigation" aria-label="main navigation">
  <div class="central-container">
    <div class="navbar-item">
      <%=  link_to "Home", homepage_home_url %>
    </div>
    <div class="navbar-item">
      <%=  link_to "Login", new_user_session_url if !user_signed_in? %>
      <%=  link_to "Signup", new_user_registration_url if !user_signed_in? %>
      <%=  link_to "Submit Word", new_panjabi_word_url if user_signed_in? %>
      <%=  link_to "Account", profile_url if user_signed_in? %>
    </div>
  </div>
</nav>
