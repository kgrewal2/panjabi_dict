<nav class="navbar is-light" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
	<a class="navbar-item" href="<%= homepage_home_url%>">
	  <h1 class="title is-5">Punjabi Dictionary</h1>
	</a>
  </div>

  <div class="navbar-menu">
	<div class="navbar-end">
	  <div class="navbar-item">
		<div class="buttons">
		  <%=  link_to "Login", new_user_session_url, class: "button is-primary"  if !user_signed_in? %>
		  <%=  link_to "Signup", new_user_registration_url, class: "button is-outlined is-primary" if !user_signed_in? %>
		  <%=  link_to "Submit Word", new_panjabi_word_url, class: "button is-primary" if user_signed_in? %>
		  <%=  link_to "Logout", destroy_user_session_url, class: "button is-outlined is-primary", :method => :delete if user_signed_in? %>
		</div>
	  </div>
	</div>
  </div>
</nav>
